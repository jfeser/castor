groupby([c_name,
	c_custkey,
	o_orderkey,
	o_orderdate,
	o_totalprice,
	sum(l_quantity)],
  [c_name,
	c_custkey,
	o_orderkey,
	o_orderdate,
	o_totalprice],
  join(c_custkey = o_custkey,
  join(o_orderkey = l_orderkey,
  customer,
  orders),
  lineitem))
