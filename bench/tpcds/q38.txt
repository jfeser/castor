select([count()],
  select([c_last_name, c_first_name, d_date],
    filter((((ss_sold_date_sk = d_date_sk) &&
            (ss_customer_sk = c_customer_sk)) &&
           ((d_month_seq <= DMS_01) && (DMS_01 <= (DMS_01 + 11)))),
      join(true, store_sales, join(true, date_dim, customer)))))

