(jbuild_version 1)

(menhir
 ((modules (parser ralgebra_parser))))

(ocamllex (lexer ralgebra_lexer))

(library
 ((name dblayout)
  (libraries (base stdio postgresql oUnit llvm llvm.analysis logs cmph))
  (preprocess (pps (ppx_sexp_conv ppx_compare ppx_hash ppx_driver.runner -no-check)))))
