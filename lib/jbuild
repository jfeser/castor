(jbuild_version 1)

(menhir
 ((modules (ralgebra_parser))))

(ocamllex (ralgebra_lexer))

(library
 ((name dblayout)
  (libraries (base core stdio postgresql oUnit llvm llvm.analysis logs cmph hashcons expect_test_helpers_kernel))
  (inline_tests)
  (preprocess (pps (ppx_jane)))
))
