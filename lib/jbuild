(jbuild_version 1)

(menhir
 ((modules (parser))))

(ocamllex (lexer))

(library
 ((name dblayout)
  (libraries (base stdio postgresql cil ounit))
  (preprocess (pps (ppx_sexp_conv ppx_compare ppx_driver.runner -no-check)))))
