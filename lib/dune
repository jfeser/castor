(rule
 (targets config.ml)
 (deps    ../config.ml)
 (action  (copy %{deps} %{targets})))

(menhir
 (modules ralgebra_parser))

(ocamllex ralgebra_lexer)

(menhir
 (modules transform_parser))

(ocamllex transform_lexer)

(library
 (public_name castor)
 (libraries base core stdio str postgresql oUnit llvm llvm.analysis llvm.target llvm.all_backends logs cmph hashcons expect_test_helpers_kernel visitors.runtime parsexp)
 (inline_tests)
 (preprocess (pps ppx_sexp_conv ppx_compare ppx_hash ppx_bin_prot ppx_let ppx_expect visitors.ppx bisect_ppx -conditional))
)

; Local Variables:
; mode: tuareg-dune
; End:
