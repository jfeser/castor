(rule
 (mode fallback)
 (targets config)
 (deps    config.defaults)
 (action  (copy %{deps} %{targets})))

(alias
 (name bench)
 (deps @install)
 (action ((run bench/demomatch/run-bench.py))))

(ignored_subdirs (bench))

; Local Variables:
; mode: tuareg-dune
; End:
