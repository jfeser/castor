$1:
  filter((l_orderkey = param0), lineitem)
  ahashidx(dedup(select([l_orderkey], lineitem)), $5, param0)
  
$2:
  select([l_shipdate], $1)
  
$4:
  join((l_orderkey = o_orderkey), lineitem, orders)
  
$5:
  filter((l_orderkey = 0.l_orderkey), lineitem)
  ahashidx(dedup(select([l_orderkey], lineitem)), $5, 0.l_orderkey)
  
